{
  "name": "@notedrop/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --var DEV_USER_ID:dev@example.com",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "deploy": "bash deploy.sh",
    "migrate": "bash deploy.sh --migrate-only"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
